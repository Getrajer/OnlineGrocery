@model AdminNavViewModel


    <div class="admin_navigation">
        <div class="admin_navigation_container">
            <div class="admin_profile">

                <div class="profile_img">
                    <div class="profile_img_container">
                        <img src="~/other_images/user.svg" alt="user_profile_img" />
                    </div>
                </div>

                <div class="profile_description">
                    <p>Mateusz Owczarek</p>
                    <p>Role: Store Owner</p>
                </div>
            </div>

            <div class="controll_list">
                <div class="controll_name">
                    <p>Main</p>
                </div>

                <ul>
                    @if (Model.LinkId == 1)
                    {
                        <li class="on_site">
                            <a class="link" asp-controller="Admin" asp-action="MainAdminPage"><i class="fas fa-home"></i> Main Window</a>
                        </li>
                    }
                    else
                    {
                        <li>
                            <a class="link" asp-controller="Admin" asp-action="MainAdminPage"><i class="fas fa-home"></i> Main Window</a>
                        </li>
                    }

                </ul>
            </div>

            <div class="controll_list">
                <div class="controll_name">
                    <p>Product</p>
                </div>

                <ul>

                    @if (Model.LinkId == 2)
                    {
                        <li class="on_site">
                            <a class="link" asp-controller="Product" asp-action="CreateProduct"><i class="fas fa-plus-circle"></i> Create Product</a>
                        </li>
                    }
                    else
                    {
                        <li>
                            <a class="link" asp-controller="Product" asp-action="CreateProduct"><i class="fas fa-plus-circle"></i> Create Product</a>
                        </li>
                    }

                    @if (Model.LinkId == 3)
                    {
                        <li class="on_site">
                            <a asp-controller="Product" asp-action="ProductsListAdmin" class="link"><i class="fas fa-list"></i> Display Products</a>
                        </li>
                    }
                    else
                    {
                        <li>
                            <a asp-controller="Product" asp-action="ProductsListAdmin" class="link"><i class="fas fa-list"></i> Display Products</a>
                        </li>
                    }

                </ul>
            </div>


            <div class="controll_list">
                <div class="controll_name">
                    <p>Users</p>
                </div>

                <ul>

                    @if (Model.LinkId == 4)
                    {
                        <li class="on_site">
                            <a asp-controller="Account" asp-action="UsersListAdmin" class="link"><i class="fas fa-users"></i> Display users</a>
                        </li>
                    }
                    else
                    {
                        <li>
                            <a asp-controller="Account" asp-action="UsersListAdmin" class="link"><i class="fas fa-users"></i> Display users</a>
                        </li>
                    }


                    <li>
                        <a class="link"><i class="fas fa-user-friends"></i> Open statistics</a>
                    </li>
                </ul>
            </div>


            <div class="controll_list">
                <div class="controll_name">
                    <p>Sales</p>
                </div>

                <ul>
                    @if (Model.LinkId == 6)
                    {
                        <li class="on_site">
                            <a asp-controller="UserOrder" asp-action="UserOrders" class="link"><i class="fas fa-user-circle"></i> User Orders</a>
                        </li>
                    }
                    else
                    {
                        <li>
                            <a asp-controller="UserOrder" asp-action="UserOrders" class="link"><i class="fas fa-user-circle"></i> User Orders</a>
                        </li>
                    }


                    @if (Model.LinkId == 7)
                    {
                        <li class="on_site">
                            <a asp-controller="Statistics" asp-action="SalesStatistics" class="link"><i class="fas fa-money-bill-wave"></i> Sales Statistics</a>
                        </li>
                    }
                    else
                    {
                        <li>
                            <a asp-controller="Statistics" asp-action="SalesStatistics" class="link"><i class="fas fa-money-bill-wave"></i> Sales Statistics</a>
                        </li>
                    }


                    <li>
                        <a class="link"><i class="fas fa-database"></i> Product Statistics</a>
                    </li>
                </ul>
            </div>

            <div class="controll_list">
                <div class="controll_name">
                    <p>CMS</p>
                </div>

                <ul>
                    @if (Model.LinkId == 9)
                    {
                        <li class="on_site">
                            <a asp-controller="CMS" asp-action="CMSIndexPage" class="link"><i class="fas fa-edit"></i> Edit Main Page</a>
                        </li>
                    }
                    else
                    {
                        <li>
                            <a asp-controller="CMS" asp-action="CMSIndexPage" class="link"><i class="fas fa-edit"></i> Edit Main Page</a>
                        </li>
                    }


                    <li>
                        <a class="link"><i class="fas fa-edit"></i> Edit Products Page</a>
                    </li>
                </ul>
            </div>

            <div class="controll_list">
                <div class="controll_name">
                    <p>Supply Chain</p>
                </div>

                <ul>
                    @if (Model.LinkId == 11)
                    {
                        <li class="on_site">
                            <a asp-controller="Suppliers" asp-action="SuppliersDisplay" class="link"><i class="fas fa-truck"></i> Suppliers</a>
                        </li>
                    }
                    else
                    {
                        <li>
                            <a asp-controller="Suppliers" asp-action="SuppliersDisplay" class="link"><i class="fas fa-truck"></i> Suppliers</a>
                        </li>
                    }


                    @if (Model.LinkId == 12)
                    {
                        <li class="on_site">
                            <a asp-controller="Suppliers" asp-action="AddSupplier" class="link"><i class="fas fa-plus-circle"></i> Add Supplier</a>
                        </li>
                    }
                    else
                    {
                        <li>
                            <a asp-controller="Suppliers" asp-action="AddSupplier" class="link"><i class="fas fa-plus-circle"></i> Add Supplier</a>
                        </li>
                    }

                    @if (Model.LinkId == 13)
                    {
                        <li class="on_site">
                            <a asp-controller="Order" asp-action="DisplayOrders" class="link"><i class="fas fa-history"></i> Orders History</a>
                        </li>
                    }
                    else
                    {
                        <li>
                            <a asp-controller="Order" asp-action="DisplayOrders" class="link"><i class="fas fa-history"></i> Orders History</a>
                        </li>
                    }

                </ul>
            </div>

            <div class="controll_list">
                <div class="controll_name">
                    <p>Organiser</p>
                </div>

                <ul>
                    <li>
                        <a class="link"><i class="fas fa-comments"></i> Team Chat</a>
                    </li>

                    <li>
                        <a class="link"><i class="fas fa-user-plus"></i> Add team member</a>
                    </li>

                    <li>
                        <a class="link"><i class="fas fa-clipboard"></i> Notes</a>
                    </li>

                    <li>
                        <a class="link"><i class="fas fa-inbox"></i> Message Board</a>
                    </li>
                </ul>
            </div>

            <div class="controll_list">
                <div class="controll_name">
                    <p>Administration</p>
                </div>

                <ul>

                    @if (Model.LinkId == 18)
                    {
                        <li class="on_site">
                            <a asp-controller="Admin" asp-action="CreateRole" class="link"><i class="fas fa-user-tag"></i> Create user role</a>
                        </li>
                    }
                    else
                    {
                        <li>
                            <a asp-controller="Admin" asp-action="CreateRole" class="link"><i class="fas fa-user-tag"></i> Create user role</a>
                        </li>
                    }

                    @if (Model.LinkId == 19)
                    {
                        <li class="on_site">
                            <a asp-controller="Admin" asp-action="ManageRoles" class="link"><i class="fas fa-cog"></i> Manage user roles</a>
                        </li>
                    }
                    else
                    {
                        <li>
                            <a asp-controller="Admin" asp-action="ManageRoles" class="link"><i class="fas fa-cog"></i> Manage user roles</a>
                        </li>
                    }


                    @if (Model.LinkId == 20)
                    {
                        <li class="on_site">
                            <a asp-controller="Admin" asp-action="CreateEmployeeAccount" class="link"><i class="fas fa-user-plus"></i> Create team account</a>
                        </li>
                    }
                    else
                    {
                        <li>
                            <a asp-controller="Admin" asp-action="CreateEmployeeAccount" class="link"><i class="fas fa-user-plus"></i> Create team account</a>
                        </li>
                    }

                </ul>
            </div>

        </div>
    </div>
